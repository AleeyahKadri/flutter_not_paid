name: flutter_not_paid
description: Client did not pay? Add this widget into your app and the app will fade away completely on reaching deadline.
version: 1.1.0
homepage: https://github.com/BirjuVachhani/flutter_not_paid/community

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.7.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ">=0.17.0 <1.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
